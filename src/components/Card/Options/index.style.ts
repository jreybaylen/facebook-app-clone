import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    cardHeaderOption: {
        top: -13,
        marginLeft: 'auto',
        position: 'relative'
    }
})

export { styles }
