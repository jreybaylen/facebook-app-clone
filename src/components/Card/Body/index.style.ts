import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    cardBody: {
        marginVertical: 5
    },
    cardBodyContent: {
        lineHeight: 20,
        marginBottom: 10
    },
    cardBodyThumbnail: {
        height: 200,
        marginBottom: 15
    }
})

export { styles }
