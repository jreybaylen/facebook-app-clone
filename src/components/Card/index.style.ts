import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    card: {
        paddingTop: 15,
        marginBottom: 15,
        borderTopWidth: 1,
        borderColor: '#ccc',
        borderBottomWidth: 1,
        backgroundColor: '#fff'
    }
})

export { styles }
