import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        borderTopWidth: 1,
        flexDirection: 'row',
        marginHorizontal: 30,
        borderTopColor: '#ccc',
    },
    action: {
        flex: 1,
        paddingVertical: 15,
        alignItems: 'center'
    }
})

export { styles }
