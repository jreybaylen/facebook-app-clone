import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    content: {
        marginBottom: 15,
        paddingVertical: 15,
        backgroundColor: '#fff'
    }
})

export { styles }
