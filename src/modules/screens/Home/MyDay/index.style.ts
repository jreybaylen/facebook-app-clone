import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    myDay: {
        paddingLeft: 15,
        marginBottom: 15,
        paddingVertical: 15,
        backgroundColor: '#fff'
    },
    myDayItem: {
        width: 100,
        padding: 15,
        height: 150,
        marginRight: 10,
        borderWidth: 0.5,
        borderRadius: 15,
        borderColor: '#000'
    }
})

export { styles }
