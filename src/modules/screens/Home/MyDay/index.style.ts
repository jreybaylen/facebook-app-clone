import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    myDay: {
        paddingLeft: 15,
        marginBottom: 15,
        paddingVertical: 15,
        backgroundColor: '#fff'
    },
    myDayItem: {
        width: 100,
        height: 150,
        borderWidth: 1,
        marginRight: 10,
        borderRadius: 15,
        overflow: 'hidden',
        borderColor: '#ccc'
    }
})

export { styles }
